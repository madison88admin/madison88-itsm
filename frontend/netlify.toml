[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  CI = "false"

[functions]
  directory = "../netlify/functions"

# Redirects are in frontend/public/_redirects (single source of truth)
